# generateMenuForCategory.js 使用指南

## 📝 简介

这是一个快速生成 `.menu.json` 文件的脚本。只需指定一个分类名称，脚本就会自动扫描该分类及其所有子目录，生成完整的菜单配置文件。

## 🚀 快速开始

### 基本用法

```bash
npm run gen-menu -- Vue
npm run gen-menu -- JavaScript
npm run gen-menu -- design-pattern
```

或直接运行：

```bash
node build/generateMenuForCategory.js Vue
```

## 📊 工作原理

1. **扫描目录** - 扫描指定分类目录及其所有子目录
2. **收集文件** - 收集所有 markdown 文件
3. **组织结构** - 将文件组织成菜单项和子菜单分组
4. **生成配置** - 生成 `.menu.json` 文件

### 生成的结构

```json
{
  "text": "分类名",
  "collapsed": true,
  "items": [
    {
      "text": "直接文章",
      "link": "/分类/文章"
    },
    {
      "text": "子目录名",
      "collapsed": true,
      "items": [
        {
          "text": "子文章",
          "link": "/分类/子目录/文章"
        }
      ]
    }
  ]
}
```

## 💡 使用示例

### 例 1：为 Vue 分类生成菜单

```bash
npm run gen-menu -- Vue
```

**输出：**

```
🚀 Generating menu for: Vue

✅ Generated: docs/Vue/.menu.json

📊 Summary:
   - Markdown files: 5
   - Subdirectories: 1
   - Total menu items: 6

📋 Menu structure:
   1. Vue官方vscode插件停止对vue2进行支持
   2. vue2中Mixin的缺点
   ...
   6. [Group] Vue3 (7 items)
```

### 例 2：为 JavaScript 分类生成菜单

```bash
npm run gen-menu -- JavaScript
```

生成的菜单包含：

- 8 个 markdown 文件
- 3 个子目录（postMessage, websocket, 闭包从入门到精通）
- 总共 11 个菜单项

### 例 3：为 design-pattern 分类生成菜单

```bash
npm run gen-menu -- design-pattern
```

生成的菜单包含：

- 9 个 markdown 文件
- 0 个子目录
- 总共 9 个菜单项

## 🎯 完整工作流程

### 第一步：为所有主要分类生成 .menu.json

```bash
npm run gen-menu -- AI
npm run gen-menu -- JavaScript
npm run gen-menu -- Vue
npm run gen-menu -- CSS
npm run gen-menu -- design-pattern
npm run gen-menu -- FrontEndEngineering
# ... 为其他分类也生成
```

### 第二步：审查和编辑生成的文件

打开 `docs/分类/.menu.json`，根据需要：

- 调整菜单项的顺序
- 修改菜单项的文本
- 改变 `collapsed` 状态

**例如，编辑 docs/Vue/.menu.json：**

```json
{
  "text": "Vue",
  "collapsed": false,  // 改为 false 表示默认展开
  "items": [
    // 调整项目顺序
    {
      "text": "Vue3",  // 修改文本
      "collapsed": true,
      "items": [...]
    }
  ]
}
```

### 第三步：生成 sidebar

```bash
npm run generate:sidebar
```

### 第四步：预览效果

```bash
npm run dev
```

打开 <http://localhost:5173/blog/> 查看效果

### 第五步：提交更改

```bash
git add docs/**/.menu.json
git commit -m "feat: add menu configurations"
git push
```

## 📋 文件命名规则

脚本会自动：

- ✅ 包含所有 `.md` 文件
- ✅ 跳过 `index.md` 和 `README.md`
- ✅ 跳过隐藏文件（以 `.` 开头）
- ✅ 按字母顺序排列
- ✅ 为子目录创建菜单分组

## 🎨 自定义菜单

### 调整菜单项文本

编辑生成的 `.menu.json` 文件，修改 `text` 字段：

```json
{
  "text": "原始名称",  // ← 修改这里
  "link": "/Vue/article"
}
```

### 调整菜单项顺序

在 `items` 数组中改变项目的顺序：

```json
{
  "items": [
    // 首先显示
    { "text": "项目A", "link": "..." },
    // 其次显示
    { "text": "项目B", "link": "..." }
  ]
}
```

### 隐藏某个菜单项

从 `items` 数组中删除该项或注释掉：

```json
{
  "items": [
    { "text": "显示的项目", "link": "..." },
    // { "text": "隐藏的项目", "link": "..." }
  ]
}
```

### 改变菜单默认状态

修改 `collapsed` 字段：

- `true` - 默认折叠
- `false` - 默认展开

```json
{
  "text": "菜单",
  "collapsed": false,  // 改为 false 表示默认展开
  "items": [...]
}
```

## 🔍 生成的菜单格式

### 单个文件

```json
{
  "text": "文章标题",
  "link": "/分类/文章标题"
}
```

### 子目录分组

```json
{
  "text": "子目录名",
  "collapsed": true,
  "items": [
    {
      "text": "子文章1",
      "link": "/分类/子目录/子文章1"
    },
    {
      "text": "子文章2",
      "link": "/分类/子目录/子文章2"
    }
  ]
}
```

## ⚙️ 命令参考

```bash
# 为指定分类生成 .menu.json
npm run gen-menu -- <category-name>

# 例如
npm run gen-menu -- Vue
npm run gen-menu -- JavaScript
npm run gen-menu -- FrontEndEngineering

# 查看帮助
node build/generateMenuForCategory.js
```

## 🆘 故障排除

### 问题 1：目录不存在

**错误信息：**

```
❌ Directory not found: E:\github-project\blog\docs\NonExistent
```

**解决：** 确保分类目录存在

```bash
# 检查目录是否存在
ls docs/Vue

# 如果不存在，先创建目录
mkdir docs/新分类
```

### 问题 2：没有 markdown 文件

**结果：** 生成的 `.menu.json` 的 `items` 为空数组

**解决：** 在该分类目录下创建 markdown 文件

### 问题 3：生成的菜单顺序不对

**原因：** 文件名排序不符合预期

**解决：** 编辑 `.menu.json` 文件，手动调整 `items` 数组的顺序

### 问题 4：链接路径错误

**症状：** 点击菜单项后页面 404

**原因：** markdown 文件不存在或路径不对

**解决：** 检查 `link` 字段是否与实际文件路径相符

## 📚 集成到 CI/CD

如果你想在部署时自动生成所有菜单，可以添加到脚本中：

```bash
#!/bin/bash
# 为所有主要分类生成菜单
npm run gen-menu -- AI
npm run gen-menu -- JavaScript
npm run gen-menu -- Vue
npm run gen-menu -- design-pattern
npm run gen-menu -- FrontEndEngineering

# 生成 sidebar
npm run generate:sidebar

# 构建
npm run build
```

## ✅ 最佳实践

### 推荐做法

1. **首次设置** - 为所有分类运行一次脚本生成初始 `.menu.json`
2. **定期更新** - 新增文章后运行脚本重新生成
3. **审查编辑** - 检查生成的文件，根据需要手动调整
4. **版本控制** - 在 git 中提交 `.menu.json` 文件
5. **自动化** - 在 CI/CD 流程中集成此脚本

### 不推荐做法

1. ❌ 不要在手动编辑后再运行脚本（会覆盖）
2. ❌ 不要删除 `.menu.json` 文件后不重新生成
3. ❌ 不要使用特殊字符（除中文/emoji）
4. ❌ 不要依赖文件的字母顺序，要手动调整

## 🎯 常见任务

### 任务 1：快速为新的分类生成菜单

```bash
# 创建新分类目录和文章
mkdir docs/新技术
touch docs/新技术/文章1.md
touch docs/新技术/文章2.md

# 生成菜单
npm run gen-menu -- 新技术

# 在 docs/.menu.json 的 categories 中添加 "新技术"

# 生成 sidebar
npm run generate:sidebar

# 预览
npm run dev
```

### 任务 2：更新现有分类的菜单

```bash
# 新增了一些文章到 docs/Vue/

# 重新生成菜单
npm run gen-menu -- Vue

# 手动检查和调整

# 生成 sidebar
npm run generate:sidebar

# 预览
npm run dev
```

### 任务 3：为多个分类一次性生成菜单

```bash
# 创建脚本 build/gen-all-menus.sh
#!/bin/bash
npm run gen-menu -- AI
npm run gen-menu -- JavaScript
npm run gen-menu -- Vue
npm run gen-menu -- CSS
npm run gen-menu -- design-pattern
npm run gen-menu -- FrontEndEngineering
npm run gen-menu -- TypeScript
npm run gen-menu -- NodeJS
npm run gen-menu -- backend

npm run generate:sidebar

# 运行
bash build/gen-all-menus.sh
```

## 💡 技巧

### 技巧 1：快速查看菜单结构

脚本会自动显示生成的菜单结构，帮助你快速了解菜单构成。

### 技巧 2：按需生成

只为需要的分类生成菜单，而不是全部生成。

### 技巧 3：保留手动编辑

生成后可以手动编辑菜单顺序、文本等，更加灵活。

## 🎉 总结

这个脚本提供了一个**快速、便捷、灵活**的菜单生成方案：

- ⚡ **快速** - 一键生成完整的菜单配置
- 📋 **完整** - 自动扫描所有子目录内容
- ✏️ **灵活** - 生成后可随意编辑和调整
- 🔄 **可重用** - 可以多次运行，覆盖已有配置

**现在就开始使用吧！** 🚀

```bash
npm run gen-menu -- Vue
npm run generate:sidebar
npm run dev
```
