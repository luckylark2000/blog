# neovim 新手常见问题

## 尝试在不存在的目录中保存文件

尝试在不存在的目录中保存文件，以`project/test.js`为例，默认会报错 `E212: Can't open file for writing: no such file or directory`，意思是说文件或者文件夹不存在，`w`、`q`、`wq`退出不了也保存不了，除非使用`:q!`强制退出，但是也会丢失数据。

### ✅ 解决方法

#### 步骤 1：先不要强制退出

你现在应该还在 Neovim 编辑界面中。如果已经看到错误提示（比如 `E212: Can't open file for writing`），没关系，继续操作即可。

#### 步骤 2：手动创建缺失的目录

在 Neovim 的命令模式下（按 `Esc` 确保处于正常模式），输入以下命令来创建 `project` 目录：

```vim
:!mkdir -p project
```

- `:!` 表示在 shell 中执行命令
- `mkdir -p` 会递归创建所有缺失的父目录（即使 `project` 不存在也会创建）

> 💡 如果你在 Windows 上，可以改用：
>
> ```vim
> :!mkdir project
> ```
>
> （PowerShell 或 CMD 支持，但 `-p` 是 Unix/Linux/macOS 的参数）

#### 步骤 3：保存文件

现在目录存在了，就可以正常写入文件：

```vim
:wq
```

这样 `project/test.js` 就会被成功保存并退出。

---

### 🔒 额外建议：避免以后再出问题

你可以安装插件（如 [`vim-auto-save`](https://github.com/907th/vim-auto-save) 或 [`auto-session`](https://github.com/rmagatti/auto-session)）。
